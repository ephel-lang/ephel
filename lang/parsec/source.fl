open std.option
open std.or

sig Source :
    sig struct
        sig e : type
        sig t : type

        sig Construct :
            sig struct
                sig c : type
                sig create : (filename:String?) -> c -> t Or String
                -- Here the variable name is just for the documentation
            end

        sig next : t -> e? * t
    end
